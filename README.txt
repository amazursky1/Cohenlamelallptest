Cohen & Lamela, LLP — GitHub Pages Optimized Build

UPLOAD INSTRUCTIONS
1. Replace the existing HTML files in your GitHub repository with the files in this package.
2. Upload the entire `images` folder at the repository root.
3. Keep `index.html` and the `images` folder at the same top level.
4. Commit the changes.
5. GitHub Pages will redeploy automatically.

IMPORTANT
Do not upload only the HTML files. The `images` folder is now required.

This build:
- uses optimized WebP files instead of large embedded base64 images;
- keeps the mobile hamburger navigation;
- keeps the desktop Practice Areas dropdown;
- keeps the expanded practice-area copy and FAQs;
- uses Criminal Law rather than Criminal Defense;
- retains the current About Us page;
- retains the firm's real attorney-photo URLs.

Current contact details:
530 Chestnut Street, Suite 203, Union, NJ 07083
908-364-2271
